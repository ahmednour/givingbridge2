# 🧪 دليل اختبار التسجيل - خطوة بخطوة

## ✅ التحقق من الإصلاح

### الخطوة 1: تشغيل التطبيق

```bash
cd frontend
flutter run
```

انتظر حتى يفتح التطبيق على الجهاز/المحاكي.

---

### الخطوة 2: الذهاب لشاشة التسجيل

1. افتح التطبيق
2. اضغط على "Register" أو "Sign Up"
3. يجب أن تظهر شاشة التسجيل

---

### الخطوة 3: إدخال البيانات

**أدخل البيانات التالية:**

```
الاسم (Name): Ahmed Test
البريد (Email): ahmed.test@example.com
كلمة المرور (Password): 123456
الدور (Role): Donor
الهاتف (Phone): 0501234567 (اختياري)
الموقع (Location): Riyadh (اختياري)
```

---

### الخطوة 4: الضغط على Register

اضغط على زر "Register" أو "Sign Up"

---

### الخطوة 5: النتيجة المتوقعة

#### ✅ إذا نجح الإصلاح:

1. **رسالة نجاح** تظهر (أو loading indicator)
2. **الانتقال التلقائي** إلى Dashboard
3. **عرض اسم المستخدم** في الأعلى
4. **عرض الدور** (Donor Dashboard)

#### ❌ إذا فشل (المشكلة القديمة):

1. **رسالة خطأ:** "Null check operator used on a null value"
2. **التطبيق يتجمد** أو يعود للشاشة السابقة
3. **لا يتم الانتقال** للـ Dashboard

---

### الخطوة 6: التحقق من قاعدة البيانات

افتح Docker logs للتحقق من نجاح التسجيل:

```bash
docker logs givingbridge_backend
```

**يجب أن ترى:**
```
[2025-11-02T12:XX:XX.XXX] INFO: Auth event: registration_success
[2025-11-02T12:XX:XX.XXX] INFO: Registration successful. Please check your email to verify your account.
[2025-11-02T12:XX:XX.XXX] INFO: Request completed
```

---

### الخطوة 7: التحقق من الملف الشخصي

1. اذهب إلى "Profile" في التطبيق
2. **تحقق من ظهور:**
   - ✅ الاسم: Ahmed Test
   - ✅ البريد: ahmed.test@example.com
   - ✅ الدور: Donor
   - ✅ الهاتف: 0501234567
   - ✅ الموقع: Riyadh

---

## 🔍 ماذا تم إصلاحه؟

### المشكلة القديمة:
```dart
// ❌ كان يفشل إذا كانت القيم null
factory User.fromJson(Map<String, dynamic> json) {
  return User(
    id: json['id'],              // ❌ قد يكون null
    createdAt: json['createdAt'], // ❌ قد يكون null
    // ...
  );
}
```

### الإصلاح الجديد:
```dart
// ✅ يتعامل مع null بشكل آمن
factory User.fromJson(Map<String, dynamic> json) {
  return User(
    id: json['id'] as int,                    // ✅ safe casting
    createdAt: json['createdAt'] as String?   // ✅ nullable
        ?? DateTime.now().toIso8601String(),  // ✅ قيمة افتراضية
    // ...
  );
}
```

---

## 📊 سيناريوهات الاختبار

### ✅ سيناريو 1: تسجيل بجميع البيانات
```
الاسم: Ahmed Test
البريد: ahmed1@example.com
كلمة المرور: 123456
الدور: Donor
الهاتف: 0501234567
الموقع: Riyadh
```
**النتيجة المتوقعة:** ✅ نجاح + انتقال للـ Dashboard

---

### ✅ سيناريو 2: تسجيل بدون هاتف وموقع
```
الاسم: Sara Test
البريد: sara@example.com
كلمة المرور: 123456
الدور: Receiver
الهاتف: (فارغ)
الموقع: (فارغ)
```
**النتيجة المتوقعة:** ✅ نجاح + انتقال للـ Dashboard

---

### ✅ سيناريو 3: تسجيل مدير
```
الاسم: Admin Test
البريد: admin@example.com
كلمة المرور: 123456
الدور: Admin
الهاتف: 0501234567
الموقع: Riyadh
```
**النتيجة المتوقعة:** ✅ نجاح + انتقال للـ Admin Dashboard

---

### ❌ سيناريو 4: تسجيل ببريد مكرر
```
الاسم: Test User
البريد: ahmed1@example.com (نفس البريد من سيناريو 1)
كلمة المرور: 123456
الدور: Donor
```
**النتيجة المتوقعة:** ❌ رسالة خطأ "Email already exists"

---

## 🎯 قائمة التحقق

- [ ] التطبيق يعمل بدون أخطاء
- [ ] شاشة التسجيل تظهر بشكل صحيح
- [ ] يمكن إدخال جميع البيانات
- [ ] زر Register يعمل
- [ ] لا يظهر خطأ "Null check operator"
- [ ] الانتقال للـ Dashboard يعمل
- [ ] بيانات المستخدم تظهر في Profile
- [ ] Docker logs تظهر نجاح التسجيل
- [ ] يمكن تسجيل الدخول بالحساب الجديد

---

## 🐛 إذا واجهت مشاكل

### المشكلة: لا يزال خطأ "Null check operator"

**الحل:**
1. تأكد من حفظ الملفات
2. أعد تشغيل التطبيق:
   ```bash
   flutter clean
   flutter pub get
   flutter run
   ```

---

### المشكلة: "Email already exists"

**الحل:**
استخدم بريد إلكتروني مختلف أو احذف المستخدم من قاعدة البيانات:
```bash
docker exec givingbridge_db mysql -ugivingbridge_user -psecure_prod_password_2024 givingbridge -e "DELETE FROM users WHERE email='ahmed.test@example.com';"
```

---

### المشكلة: التطبيق لا يتصل بالباك إند

**الحل:**
1. تأكد من تشغيل Docker:
   ```bash
   docker ps
   ```
2. تأكد من عنوان API في `api_service.dart`:
   ```dart
   static const String baseUrl = 'http://localhost:3000';
   ```

---

## ✅ التأكيد النهائي

إذا نجحت جميع الخطوات أعلاه، فإن الإصلاح **يعمل بشكل صحيح 100%** ✨

**المشروع جاهز للعرض التقديمي!** 🎓

---

**تاريخ الاختبار:** 2 نوفمبر 2025  
**الحالة:** ✅ جاهز للاختبار
